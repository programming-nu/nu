;; Nukefile for Nu demo application

;; source files
(set @nu_files 	  (filelist "^nu/.*nu$"))

;; application description
(set @application 	       	   "Console")
(set @application_identifier   "nu.programming.console")

;; tasks
(application-tasks)
(task "default" => "application")
